# Scripts

## Windows (PowerShell)

No necesitas WSL ni Git Bash. Desde la raíz del proyecto:

```powershell
.\scripts\start-app.ps1
.\scripts\run-tests.ps1
```

## Linux / Mac / Git Bash

```bash
bash scripts/start-app.sh
bash scripts/run-tests.sh
```
